<section class="education" id="education🎓">
	<h2>Education</h2>
	<% if (education.length === 0) {%>
		<%- include("error.ejs", {
			msg: "Could not fetch my education info, or maybe I did not finish any school."
		}) -%>
			<%} else {%>
				<div class="education__timeline_wrapper">
				<div class="education__timeline">
				
			<% education.forEach(edu => { %>
				<article>
					<h5>
						<%= edu.school %>
					</h5>
					<p><code>
						<%= edu.startDate.toLocaleDateString() %> - <%= edu.finishDate.toLocaleDateString() %>
					</code></p>
					<p>
						<%= edu.moreInfo %>
					</p>
				</article>
				<% }) %>
			</div>
	</div>
	<% } %>
</section>